let screen  = document.getElementById('inputscr');
let buttons = document.querySelectorAll('button');

let string = '';


let arr = Array.from(buttons);
arr.map(button =>{
    button.addEventListener('click',(val)=>{
        if(val.target.innerHtml == '=')
    })
})